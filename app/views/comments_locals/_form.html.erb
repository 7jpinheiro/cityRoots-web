<%= form_for(@comments_local) do |f| %>
  <% if @comments_local.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comments_local.errors.count, "error") %> prohibited this comments_local from being saved:</h2>

      <ul>
      <% @comments_local.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="field">
    <%= f.label :evalutiondate %><br>
    <%= f.date_select :evalutiondate %>
  </div>
  <div class="field">
    <%= f.label :user_mobile_id %><br>
    <%= f.number_field :user_mobile_id %>
  </div>
  <div class="field">
    <%= f.label :local_id %><br>
    <%= f.number_field :local_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
