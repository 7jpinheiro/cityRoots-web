<%= form_for(@local_attraction) do |f| %>
  <% if @local_attraction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@local_attraction.errors.count, "error") %> prohibited this local_attraction from being saved:</h2>

      <ul>
      <% @local_attraction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :details %><br>
    <%= f.text_area :details %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :attraction_type %><br>
    <%= f.number_field :attraction_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
