<%= form_for(@local_service) do |f| %>
  <% if @local_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@local_service.errors.count, "error") %> prohibited this local_service from being saved:</h2>

      <ul>
      <% @local_service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="field">
    <%= f.label :details %><br>
    <%= f.text_area :details %>
  </div>
  <div class="field">
    <%= f.label :service_type_id %><br>
    <%= f.number_field :service_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
