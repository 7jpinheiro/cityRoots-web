<%= form_for(@attraction_type) do |f| %>
  <% if @attraction_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attraction_type.errors.count, "error") %> prohibited this attraction_type from being saved:</h2>

      <ul>
      <% @attraction_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :attraction_big_types_id %><br>
    <%= f.number_field :attraction_big_types_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
