<%= form_for(@comments_event) do |f| %>
  <% if @comments_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comments_event.errors.count, "error") %> prohibited this comments_event from being saved:</h2>

      <ul>
      <% @comments_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="field">
    <%= f.label :evalutiondate %><br>
    <%= f.date_select :evalutiondate %>
  </div>
  <div class="field">
    <%= f.label :mobile_user_id %><br>
    <%= f.number_field :mobile_user_id %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
